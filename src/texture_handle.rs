//! Handles to various texture atlases
use bevy::prelude::*;

/// Code Page 437
pub struct Cp437(pub Handle<TextureAtlas>);

/// Placeholder player sheet
pub struct PlaceholderPlayer(pub Handle<TextureAtlas>);
